# !--------------------2D cone experiment----------------------------------------
#DIR="sim_DiffuseTube_with_velocity"

DIR="sod_two_slab"

./setup -auto Sod_two_slab -2d -nxb=16 -nyb=16 -parfile=slabs_2d.par -objdir=data/$DIR

cd data/$DIR

make 


